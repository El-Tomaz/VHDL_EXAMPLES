# Logic Gates

In this example, the following combinational circuit is built:

![fig]()

With this exercise, the basic sintax of VHDL could be praticed, specially the use of logic operators.

To run this example use:

`
ghdl -a ./*.vhd*
ghdl -r tb --vcd=wave.vcd
gtk wave.vcd

`
